# expo-module-scripts invokes its tool binaries (eslint, tsc, jest) via `pnpm exec`.
# pnpm's default isolated node_modules doesn't expose those transitive binaries at
# the project root, so use npm-style hoisting to make them resolvable in dev + CI.
node-linker=hoisted
